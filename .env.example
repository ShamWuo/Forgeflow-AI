# Rename this file to .env.local and provide your credentials
OPENAI_API_KEY=
# Optional: point to a custom OpenAI-compatible endpoint
OPENAI_BASE_URL=
# Enable to bypass live model calls during development
NEXT_PUBLIC_ENABLE_MOCKS=false
